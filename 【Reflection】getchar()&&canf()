//这是一篇分析总结文

/*现要求输入N(N<100)的100以内的正整数。
并且输入时，第一个数单独占一行。
后面所有数都占据第二行，且第二行的每次输入的数字之间有个空格。
最后从后到前在一行里输出所有数字。

输入时格式如下:
6
4
-6 8 12 -34 24
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int N, i;
    scanf("%d",&N);
    int a[N];
    scanf("%d",&a[0]);
    for(i=1; i<N; i++)
    {
        scanf("%d",&a[i]);
        getchar();
    }

    for(i=N-1; i>=0; i--)
      printf("%d ",a[i]);
    return 0;
}
